name: cepu_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0
  path: ^1.9.0
  image_picker: ^1.0.7
  flutter_image_compress: ^2.3.1
  geolocator: ^12.0.0
  firebase_storage: ^13.3.0
  url_launcher: ^6.3.0
  share_plus: ^12.0.2
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  http: ^1.6.0
  firebase_messaging: ^15.2.4
  flutter_local_notifications: ^18.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
